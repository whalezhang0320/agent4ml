"""Agent4ML TUI 统一主题色板。

CodeMaker 参考设计的配色思路：基底用纯中性灰（深灰 → 浅灰的层次过渡），
不带蓝/紫色偏色；色彩只用于有限几个高饱和强调色（左竖线/状态标记），
不掺进背景、边框、正文这些"基底"里。之前的版本把 #0e0e13/#1c1c28 这类
偏蓝紫的深色当基底，看起来阴沉发黑发紫；这里改成纯灰阶基底，强调色收窄
到只做点缀，视觉上更接近浅色调、通透的效果。

各 widget 模块从此处导入色值，禁止再散落硬编码 hex，保证全局一致。
"""

from __future__ import annotations

# ---- 基底：纯中性灰阶，深灰→浅灰过渡，不带色偏 ----
BG = "#121212"              # 背景：中性深灰
SURFACE = "#1e1e1e"          # 卡片/输入框填充：比背景亮一档的中性灰
SURFACE_ALT = "#242424"     # 次级填充（状态行/信息行/侧面板背景）：再亮一档

# ---- 边框：中性灰，比背景亮，给出清晰边界 ----
BORDER = "#3a3a3a"
BORDER_FOCUS = "#7c6ff0"    # 聚焦态改用强调色，仅此一处用彩色边框

# ---- 文本：中性灰阶，不带色偏 ----
TEXT_PRIMARY = "#e8e8e8"    # 主文本（用户输入/回答正文）：足够亮
TEXT_SECONDARY = "#a0a0a0"  # 次要文本（标签/说明）
TEXT_DIM = "#6e6e6e"        # 弱化文本（版本号/次级提示）

# ---- 强调色（分层但收敛，不铺满全屏） ----
ACCENT_USER = "#7c6ff0"        # 用户消息左竖线：明亮蓝紫
ACCENT_ASSISTANT = "#45c4b8"   # 助手消息左竖线：青绿，和用户区分
ACCENT_BRAND = "#a08cf5"       # 品牌强调（副标题/面板标题）
ACCENT_THOUGHT = "#FF8C42"     # 思考态：保留原橙色（执行状态色不变）
ACCENT_WARN = "#f0a868"        # 临界值提醒：暖橙，不用刺眼红
TOOL_BLUE = "#5aa9f5"          # 工具调用行强调

# ---- 面板明细标记色（语义分层，收敛在同一冷色家族） ----
MARK_WINDOW = "#5aa9f5"
MARK_MODEL = "#45c4b8"
MARK_MODE = "#a08cf5"
MARK_MCP = "#f0c05a"
MARK_FREE = "#6fce8a"

# ---- 进度条 ----
BAR_FILL = "#7c6ff0"
BAR_EMPTY = "#2c2c2c"
